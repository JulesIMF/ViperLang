{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/tests/tests",
            "args": [],
            "cwd": "${workspaceFolder}"
        }

    ],

}